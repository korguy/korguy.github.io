<div class="container">
  <header class="page-head">
    <h1 class="page-title">Work</h1>
    <p class="page-meta">Publications and projects, newest first.</p>
  </header>

  <div class="cards">
    <% (site.data.work || []).forEach(function (item) { %>
      <%- partial('partial/work-card', {item: item}) %>
    <% }) %>
  </div>
</div>
